// _.component() 的設定

const CompSetting = {
    // 採用手機, history 只有 back(),沒有 next()
    // 避免留下無用的垃圾資料
    noHistoryNextAction: true,
};



//-------------------------------------------
export { CompSetting };
export default CompSetting;
